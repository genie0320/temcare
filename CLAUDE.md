# 올라케어 (TEM 64체질) — 프로젝트 컨텍스트

> 이 파일은 세션 시작 시 자동으로 읽힙니다. 작업 전 `docs/` 를 먼저 확인하세요.

## 1. 이 서비스가 하는 일

문진으로 64개 체질 중 하나를 판별하고, 그 체질에 맞는 **행동 처방**(영양 → 식이 → 생활 → 약재)을 제공한다.

**진짜 목적은 협력 한의원으로 사람을 보내는 깔때기다.** 앱에서는 30~40문항 약식 문진과 약식 결과를 제공하고, "정확히 알고 싶으면 협력 한의원에서 130문항 정밀 문진"으로 유도한다. 정밀 문진과 판별 알고리즘은 **외부 시스템(준차트, 타 개발사)** 소관이며 우리는 문항을 받아 렌더링하고 응답을 넘긴 뒤 **결과값(1~64 정수) 하나를 돌려받는다.**

이 구조를 이해하는 것이 중요하다. 우리가 만드는 가치는 판별이 아니라 **그 정수 하나를 받아 무엇을 보여주는가**에 전부 있다.

## 2. 절대 어기면 안 되는 것 (Non-negotiable)

### 2-1. 감사로그와 권한은 1일차에 데이터 접근 계층에 심는다

건강정보 = **민감정보**를 다루는 서비스다. 개인정보 접속기록은 법적으로 **민감정보 처리 시 2년 보관 + 월 1회 이상 점검** 의무가 있다.

- **모든 데이터 변경은 `audit_log`에, 모든 개인정보 열람은 `access_log`에** 기록된다.
- 이것은 화면 기능이 아니라 **횡단 관심사**다. 리포지토리/서비스 계층에서 자동 기록되게 만들고, 각 핸들러가 개별적으로 호출하는 구조로 만들지 말 것.
- **나중에 부착이 불가능하다.** 첫 커밋부터 들어가야 한다.
- 권한도 같다. 역할 × 리소스 × 행위 검사가 라우트 진입 지점에서 일괄 적용되어야 하며, **개인정보 열람 권한은 콘텐츠 편집 권한과 분리**한다(에디터는 콘텐츠만, CS는 회원정보만).

### 2-2. 판별 API는 어댑터 뒤에 둔다

준차트 연동은 아직 스펙이 없고 일정도 외부에 달려 있다. 1차는 **mock 판별**(임의의 1~64 반환)로 전체 플로우가 돌아야 한다.

```
DiagnosisProvider (interface)
├── MockDiagnosisProvider      ← 1차. 임의 1~64 반환, 지연 시뮬레이션
└── JunchartDiagnosisProvider  ← 2차. 실연동 시 이것만 추가
```

**판별 결과를 다루는 코드가 준차트의 존재를 알면 안 된다.** 설정으로 교체 가능해야 한다.

### 2-3. 사용자가 입력해 쌓이는 기능은 만들지 않는다

트래커·무드·케어포인트는 **의도적으로 보류**다. 개발량 때문이 아니라 **되돌릴 수 없어서**다. 한 번 기록이 쌓이면 방향 전환 시 지울 수 없다(지우면 사용자가 자기 기록을 잃는다).

명세서에서 `단계 = 보류`인 화면은 **테이블도 만들지 말 것.** 스키마에 미리 넣어두고 싶은 유혹이 있지만, 빈 테이블은 "곧 만들 것"이라는 신호가 되어 범위를 다시 부풀린다.

### 2-4. 처방 데이터는 약점 태그로 자동 구성된다

고객 처방 화면의 내용은 **체질별로 수동 큐레이션하지 않는다.** 체질에 붙은 약점 태그로 영양·식이·생활·약재를 자동으로 끌어온다. 예외는 `tem_type_curation` 테이블로 관리하는 영양·약재·식품군 노출 선택뿐이다.

## 2-5. 기술 스택 (요약)

정본은 `docs/08_tech_stack.md`. 요약하면:

- **백엔드** Django + DRF, **DB** PostgreSQL(국내 리전). Django Admin·Template으로 화면을 만들지 않는다 — JSON API만 낸다.
- **프론트** React + Vite + TypeScript. 관리자와 고객 모두 웹. **1차는 네이티브 앱이 아니다.**
- **인증** 세션 쿠키(HttpOnly·Secure·SameSite=Lax). 토큰을 브라우저 저장소에 넣지 않는다.
- **`AUTH_USER_MODEL`은 하나로 통합**한다 — 고객·운영자 공용 `accounts.User` + `AdminProfile`(1:1). SQL의 `admin_account`는 이 형태로 반영한다.
- 스타일은 Tailwind + `tokens.ts` 단일 소스. **토큰만 나중에 RN으로 넘어가고 클래스는 버린다 — 의도된 선택이다.**

원칙은 **빠르게 구현하고 빠르게 확인하기**다. 나중의 RN 전환을 위해서는 "전환 시 오류를 줄이는" 최소한(`core`/`ui` 분리, 토큰 단일 소스, 웹 API 어댑터, flex 우선)만 지불하고, **"전환 비용을 0으로 만드는" 투자는 하지 않는다.** 이 제약은 **고객 프론트에만** 적용하며 관리자에는 걸지 않는다.

## 3. 1차 범위

`docs/01_scope_phase1.md` 가 정본이다. 요약하면:

- **고객 30화면** — 온보딩/동의, 문진(mock), 결과, 처방 스트림 4층, **협력 한의원 목록**, 정책·계정 필수
- **관리자 25화면** — 콘텐츠 마스터(프로토타입에 이미 구현됨), 회원·동의·약관·권리요청·파기·권한·감사로그, 한의원 마스터
- **1차 완료 기준**: mock 판별로 `가입 → 문진 → 결과 → 처방 → 한의원 연결` 한 바퀴가 실제로 돈다.

명세서 xlsx의 `단계` 열을 `1차`로 필터링하면 그대로 작업 목록이 된다.

**완성도의 기준은 환자용이 아니라 영업용이다.** 원장이 다른 원장에게 보여주며 "우리 앱이 이렇게 환자를 보냅니다"라고 설명할 수 있으면 1차는 끝이다.

## 4. 참조 자료 사용법

| 파일 | 용도 |
|---|---|
| `spec/temcare_admin-screen-spec_v3.xlsx` | 관리자 화면 정본. 시트2에 화면별 UI 요소, 시트3에 변경 근거 |
| `spec/temcare_customer-screen-spec_v5.xlsx` | 고객 화면 정본. 동일 구조 |
| `spec/ollacare_mobile-front-review_v3.pptx` | **고객 화면설계서(와이어프레임+주석).** M2 화면의 시각적 기준. 단 2026-07-23판이라 7/31 결정들이 빠져 있다 — **채택 범위는 `docs/06_decisions.md` #25에 고정** |
| `prototype/admin_prototype.html` | **관리자 화면의 시각적·동작 기준.** 브라우저로 열면 실제로 CRUD가 동작한다(sql.js). 새 관리자 화면은 이 룩앤필과 상호작용을 따를 것 |
| `prototype/prescription_stream_mockup.html` | **고객 처방 스트림의 디자인 기준** |
| `prototype/ollacare.sqlite` | 시드 데이터. 콘텐츠 마스터 구조를 실제 데이터로 확인할 때 |
| `schema/*.sql` | 데이터 모델 |
| `docs/08_tech_stack.md` | **기술 스택·개발 환경 정본.** 스택 선택 근거, 인증, 감사로그 구현, 빠른 확인 환경, 보안 체크리스트 |
| `docs/*.md` | 아래 참조 |

프로토타입 HTML은 **참조 구현**이지 이식 대상이 아니다. sql.js 기반 단일 파일이라 실서비스 코드로 옮길 수 없다. 가져올 것은 **화면 구조·필드 정의·상호작용 규칙·디자인 토큰**이며, `docs/04_design_system.md`와 `docs/05_screen_conventions.md`에 추출해 두었다.

## 5. 작업할 때

- **화면을 만들기 전에 명세서에서 해당 행을 먼저 읽을 것.** 필드와 데이터소스가 정해져 있다.
- 명세서와 프로토타입이 어긋나면 **명세서가 우선**이다(프로토타입이 더 오래됐을 수 있음).
- 명세서에 없는 화면·필드를 임의로 추가하지 말 것. 필요하다고 판단되면 먼저 제안하고 확인받는다.
- `📌` 표시는 **미확정 사항**이다. 그 부분에 부딪히면 임의로 정하지 말고 물어볼 것.
- 커밋 전에 `docs/02_architecture_constraints.md`와 `docs/08_tech_stack.md` §9의 체크리스트를 확인한다.
- **감사로그 우회 4종 금지**: `QuerySet.update()` · `QuerySet.delete()` · `bulk_create/bulk_update` · 원시 SQL. ORM 시그널이 뜨지 않아 감사로그에 구멍이 생긴다.

## 6. 용어

| 용어 | 의미 |
|---|---|
| TEM | 체온(Temperature) · 먹고싸고(Eat/Excrete) · 멘탈(Mental) 세 축. 체질 감별의 기준 |
| 64체질 / 64유형 | 판별 결과. `tem_type` |
| 약점(태그) | 체질이 가진 취약 지점 10개(추위·소화불량·변비·설사·부종·불면·면역·간해독·항진·불안우울). **모든 콘텐츠 연결의 축** |
| IDEA | 약점 마스터의 또 다른 타입. 이데아끼리 조합되는 예외 존재(TEM54) → 개수 검증 금지 |
| 캐치프레이즈 | 약점의 감성 별칭(예: '똥 막힌 하수도'). 고객 처방 화면 그룹 제목에 사용 |
| 관점 / 효능기전 | 같은 영양소·약재라도 다른 각도의 설명. 카드 단위 = (항목 × 관점) |
| 인생처방 | 고객에게 노출되는 **약재** 처방. 방제(고방/후세방)가 아님 |
| 처방 스트림 | 영양→식이→생활→약재 4정거장 크레센도 구조의 고객 처방 화면 |
| 요법관리 | 구 '체질별 관리법'. `article` 테이블 |
| 준차트 | 정밀 문진·판별을 담당하는 외부 시스템(타 개발사) |
| 템라이프 | 콘텐츠 피드. **2차로 이연됨** |

## 7. 체질은 변하지 않는다

원장 의론의 출발점이다. 체질은 타고나며 평생 바뀌지 않는다. 따라서:

- **재검사 결과가 달라지는 것은 "체질 변화"가 아니라 약식 문진의 흔들림**이다. 이를 이력으로 노출하지 않는다.
- 리텐션 설계를 "다시 검사하게 만들기"에 걸지 않는다. 이 앱은 매일 쓰는 도구가 아니라 **평생 쓰는 내 몸 사용설명서**에 가깝다.
