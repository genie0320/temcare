# Python
backend/.venv/
__pycache__/
*.pyc
# db.sqlite3* — 재시드 전에 뜨는 .bak 파일까지 같이 막는다(실제로 하나 올라올 뻔했다).
backend/db.sqlite3*
backend/.env
backend/media/
.pytest_cache/

# Node / Vite
node_modules/
dist/
.vite/
admin-web/.env
app-web/.env

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code — 이 컴퓨터에서만 쓰는 권한 설정, 공유 저장소엔 안 올린다
.claude/settings.local.json
